# Port configuration for TAC WebBuilder
# Copy this file to .ports.env and customize if needed

# Backend API server port
BACKEND_PORT=8002

# Frontend development server port (standard Vite port)
FRONTEND_PORT=5173

# Backend URL for Vite proxy
VITE_BACKEND_URL=http://localhost:8002
