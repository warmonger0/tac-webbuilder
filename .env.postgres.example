# PostgreSQL Testing Configuration Example
# Copy this file to .env.postgres and update values as needed
# Usage: export $(cat .env.postgres | xargs) && run_tests

DB_TYPE=postgresql
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=tac_webbuilder
POSTGRES_USER=tac_user
POSTGRES_PASSWORD=changeme
POSTGRES_POOL_MIN=2
POSTGRES_POOL_MAX=10
POSTGRES_TIMEOUT=30
